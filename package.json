{
  "name": "server",
  "version": "1.0.0",
  "description": "This is the backend part of a MERN (MongoDB, Express.js, React, Node.js) web application that serves as an intermediary for fetching top stories from the New York Times API. It provides an API endpoint for the frontend to fetch the data.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Wasif Nadeem",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.5.1",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
